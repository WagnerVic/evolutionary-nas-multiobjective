# --- Núcleo de busca / otimização ---
numpy>=1.26
pymoo>=0.6.1            # NSGA-II, hipervolume, IGD (F3)

# --- Treino de CNNs (pipeline F1) ---
torch>=2.2             # construtor de CNN, params via .numel()
torchvision>=0.17      # Fashion-MNIST / MNIST

# --- Medição de custo do modelo ---
ptflops>=0.7.3         # FLOPs/MACs (f3). Alternativas: thop, fvcore

# --- Diferencial D6 (energia) ---
codecarbon>=2.3        # estimativa de kWh / CO2eq

# --- Análise estatística e figuras (F5) ---
scipy>=1.12            # Mann-Whitney U, Wilcoxon
pandas>=2.2            # tabelas de resultados (results/*.csv)
matplotlib>=3.8
seaborn>=0.13          # boxplot/violino, coordenadas paralelas

# --- Diferencial D5 (mutação via LLM) ---
# Descomente o provedor que a equipe for usar:
# anthropic>=0.40
# openai>=1.40
